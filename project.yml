name: Template
groupSortPosition: top
configFiles:
  Debug: xcconfigs/Debug.xcconfig
  Release: xcconfigs/Release.xcconfig
targets:
  Template:
    type: application
    platform: iOS
    deploymentTarget: "10.0"
    sources: [Template]
    configFiles:
        Debug: xcconfigs/Template-Debug.xcconfig
        Release: xcconfigs/Template-Release.xcconfig
    dependencies:
        - carthage: XCGLogger
  TemplateTests:
    type: bundle.unit-test
    platform: iOS
    sources: [TemplateTests]
    configFiles:
        Debug: xcconfigs/TemplateTests-Debug.xcconfig
        Release: xcconfigs/TemplateTests-Release.xcconfig
    dependencies:
      - target: Template
  TemplateUITests:
    type: bundle.ui-testing
    platform: iOS
    sources: [TemplateUITests]
    configFiles:
        Debug: xcconfigs/TemplateUITests-Debug.xcconfig
        Release: xcconfigs/TemplateUITests-Release.xcconfig
    dependencies:
      - target: Template
